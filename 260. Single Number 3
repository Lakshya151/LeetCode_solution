class Solution {
public:
    vector<int> singleNumber(vector<int>& nums) {
        int n=nums.size();
        vector<int>ans;
        multiset<int>s(s);
        for(int i=0;i<nums.size();i++){
            s.insert(nums[i]);
        }
        for(int x:s){
            if(s.count(x)==1){
                ans.push_back(x);
            }
        }
        return ans;
    }
};
