class Solution {
public:
    string largestEven(string s) {
        if(s.size()==1 && s[0]=='1'){
            return "";
        }
        while(!s.empty()&&s.back()=='1'){
            s.pop_back();
        }
        return s;
    }
};
